services:
  - type: web
    name: print-pilot-shop
    runtime: static
    buildCommand: cd print-pilot && npm ci && npm run build
    staticPublishPath: print-pilot/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
