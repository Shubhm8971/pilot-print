services:
  - type: web
    name: print-pilot-shop
    runtime: static
    rootDir: print-pilot
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
